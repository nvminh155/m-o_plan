import { Stack } from "expo-router";
import React from "react";

const PlansLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    ></Stack>
  );
};

export default PlansLayout;
